# Environment variables example for FinShadow

# ==================== Runtime ====================
NODE_ENV=development
PORT=5000

# ==================== Database (REQUIRED) ====================
# PostgreSQL connection string for Drizzle ORM
# Development example: postgres://user:password@localhost:5432/finshadow
# Production: Use secure, long-lived credentials from your cloud provider
DATABASE_URL=postgres://user:password@localhost:5432/finshadow

# ==================== Authentication ====================
# JWT secret for signing authentication tokens
# IMPORTANT: Change this in production to a strong random string (min 32 chars)
# Generate: openssl rand -base64 32
JWT_SECRET=dev-secret-key-change-in-production

# ==================== OSINT Data Sources ====================
# Optional: AlienVault OTX API key for threat intelligence ingestion
# Sign up at: https://otx.alienvault.com/accounts/register
# Leave empty to use public/free tier (limited rate)
ALIENVALULT_OTX_API_KEY=

# ==================== Replit Deployment (Optional) ====================
# Only set these if deploying on Replit
# REPLIT_INTERNAL_APP_DOMAIN=your-app.internal.repl.co
# REPLIT_DEV_DOMAIN=your-app.dev.repl.co
# REPL_ID=your-repl-id

# ==================== Notes ====================
# 1. Never commit actual .env file (it has real secrets)
# 2. Use .env.example as template for developers
# 3. In production, set env vars via deployment platform or secure vault
# - Do not commit real secrets. Use this file as a template and create a `.env` file
#   with real values locally or set environment variables in your deployment.
# - Drizzle CLI will read `DATABASE_URL` to connect to the database when running
#   `npm run db:push` or other Drizzle commands.
